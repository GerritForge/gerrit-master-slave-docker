[replication]
    lockErrorMaxRetries = 3

[remote "slave"]
    url = http://slave:8888/${name}.git
    adminUrl = ssh://slave/var/gerrit/git/${name}.git
    push = +refs/*:refs/*
    timeout = 30
    replicationDelay = 1
    threads = 4
    replicateProjectDeletions = true
    mirror = true
    
